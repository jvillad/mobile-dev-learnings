import { StyleSheet, Platform, StatusBar } from 'react-native';

export default StyleSheet.create({
    mainBtnBorder: {
        backgroundColor: '#ef4444', // jasper
        padding: 10,
        borderRadius: 5,
    },

    mainBtnText: {
        color: '#FDFDFF', // white
        textAlign: 'center',
        fontWeight: 'bold',
        fontSize: 16,
    },
});
